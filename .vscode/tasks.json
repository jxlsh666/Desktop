{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "build debug",
			"linux":{
				"command": "mkdir -p build && cmake -B build -DCMAKE_BUILD_TYPE=Debug && cd build && make",
			}
		}
	]
}